{
  "name": "server",
  "type": "module",
  "main": "dist/index.mjs",
  "scripts": {
    "build": "tsdown",
    "check-types": "tsc -b",
    "compile": "bun build --compile --minify --sourcemap --bytecode ./src/runtime.ts --outfile server",
    "dev": "bun run --hot src/runtime.ts",
    "start": "bun run dist/index.mjs"
  },
  "dependencies": {
    "@hono/trpc-server": "^0.4.0",
    "@omniskill/api": "workspace:*",
    "@omniskill/auth": "workspace:*",
    "@omniskill/db": "workspace:*",
    "@omniskill/env": "workspace:*",
    "@trpc/server": "catalog:",
    "better-auth": "catalog:",
    "dotenv": "catalog:",
    "hono": "catalog:"
  },
  "devDependencies": {
    "@omniskill/config": "workspace:*",
    "@types/bun": "catalog:",
    "tsdown": "^0.16.5",
    "typescript": "catalog:"
  }
}
