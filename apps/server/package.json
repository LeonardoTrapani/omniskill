{
  "name": "server",
  "type": "module",
  "main": "dist/index.mjs",
  "scripts": {
    "build": "tsdown",
    "check-types": "tsc -b",
    "compile": "bun build --compile --minify --sourcemap --bytecode ./src/runtime.ts --outfile server",
    "dev": "bun run --hot src/runtime.ts",
    "start": "bun run dist/index.mjs"
  },
  "dependencies": {
    "@better-skills/api": "workspace:*",
    "@better-skills/auth": "workspace:*",
    "@better-skills/db": "workspace:*",
    "@better-skills/env": "workspace:*",
    "@hono/trpc-server": "^0.4.0",
    "@trpc/server": "catalog:",
    "better-auth": "catalog:",
    "dotenv": "catalog:",
    "hono": "catalog:"
  },
  "devDependencies": {
    "@better-skills/config": "workspace:*",
    "@types/bun": "catalog:",
    "tsdown": "^0.16.5",
    "typescript": "catalog:"
  }
}
