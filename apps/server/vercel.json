{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "hono",
  "installCommand": "bun install",
  "buildCommand": "if [ \"$VERCEL_ENV\" = \"production\" ]; then bun run db:migrate; fi && bun run --filter server build"
}
